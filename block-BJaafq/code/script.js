function addNum(numA = 0, numB = 0) {
    return numA + numB;
}

function testAddOne() {
    result = addNum(1, 2);
    expected = 3;
    if (typeof numA !== 'number' || typeof numB !== 'number') {
        throw new Error(``)
    }
}